import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container:{
    flex: 1,
    alignItems: 'center',
  },

  sadFace: {
    marginTop: 30,
    width: 130,
    height: 130,
  },

  errorText: {
    fontSize: 22,
    marginTop: 5
  },

  linkText: {
    color: '#1EB36B',
    fontWeight: 'bold'
  }
});

export default styles;